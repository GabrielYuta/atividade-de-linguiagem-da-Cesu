#include <stdio.h>
#include <stdlb.h>

 int main (int argc, char *argv[] ){
 
    int base, altura;
    float area;
    
    base = 10;
    altura = 20;
    
    area = (base*altura)/2;
    
    printf("A area do Triangulo Retangulo do Base = %d e altura e %f" , base, altura, area);

 returm 0;
 }
